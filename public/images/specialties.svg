<?xml version="1.0" encoding="utf-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     width="1500" height="700" viewBox="0 0 1500 700" role="img" aria-label="Specialties">
  <!-- Transparent background (no rect) -->
  <title>Specialties</title>

  <!-- Optional: set a small margin by using a group with transform if needed -->
  <g>
    <!-- Minimal centered text -->
    <text
      x="50%" y="50%"
      fill="#EEEEEE"
      font-family="system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif"
      font-weight="300"
      font-size="200"
      letter-spacing="3"
      text-anchor="middle"
      dominant-baseline="middle">
      Specialties
    </text>
  </g>
</svg>
